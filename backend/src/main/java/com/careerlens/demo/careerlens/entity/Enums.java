package com.careerlens.demo.careerlens.entity;

public class Enums {
    public enum UserRole { JOB_SEEKER, RECRUITER, ADMIN }
    public enum ReadinessLevel { HIGH, MEDIUM, LOW }
    public enum ApplyDecision { APPLY_NOW, APPLY_AFTER_PREP, LONG_TERM_PREP }
    public enum ApplicationStatus { PREPARING, SUBMITTED, DOCUMENT_PASSED, INTERVIEW, FINAL_RESULT }
    public enum VerificationStatus { PENDING, APPROVED, REJECTED }
}
